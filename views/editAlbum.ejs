<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= album.id ? 'Edit' : 'Add' %> Album</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <header class="navbar navbar-dark sticky-top bg-primary flex-md-nowrap p-0 shadow">
        <a class="navbar-brand col-md-3 col-lg-2 me-0 px-3" href="#">Track Management</a>
        <button class="navbar-toggler position-absolute d-md-none collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-nav">
            <div class="nav-item text-nowrap">
                <a type="button" class="px-3 btn btn-danger" href="/login">Logout</a>
            </div>
        </div>
    </header>

    <div class="container-fluid">
        <div class="row">
            <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-primary sidebar collapse">
                <div class="position-sticky pt-3">
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link active" href="/albums">
                                <span data-feather="list"></span>
                                Manage Albums
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/albums/new">
                                <span data-feather="plus-circle"></span>
                                Add New Album
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/tracks">
                                <span data-feather="list"></span>
                                Manage Tracks
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/tracks/new">
                                <span data-feather="plus-circle"></span>
                                Add New Track
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>

            <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
                <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                    <h1 class="h2"><%= album.id ? 'Edit' : 'Add' %> Album</h1>
                </div>

                <form action="<%= album.id ? `/albums/${album.id}` : '/albums' %>" method="POST">
                    <% if (album.id) { %>
                        <input type="hidden" name="_method" value="PUT">
                    <% } %>

                    <div class="mb-3">
                        <label for="album_name" class="form-label">Album Name</label>
                        <input type="text" class="form-control" id="album_name" name="album_name" value="<%= album.album_name || '' %>" required>
                    </div>

                    <a href="/" class="btn btn-secondary mb-3">Back to Index</a>

                    <button type="submit" class="btn btn-primary"><%= album.id ? 'Update' : 'Add' %> Album</button>
                </form>
            </main>
        </div>
    </div>

    <script src="/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/feather-icons@4.28.0/dist/feather.min.js"></script>
</body>
</html>
